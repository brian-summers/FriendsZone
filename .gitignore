node_modules/
dist/
coverage/
*.tsbuildinfo

# Secrets — never commit. See docs/security/secrets.md
.env
.env.*
!.env.example
*.pem
*.key

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
.claude/
.editorconfig

# Durable store (ADR 0025). Holds password and session hashes — keep it out of
# backups that are less protected than a database would be.
.data/
