{
  "name": "@friendszone/infra",
  "version": "0.0.0",
  "private": true,
  "description": "CDK app for the Friendszone AWS deployment. Deliberately NOT an npm workspace member - `npm run verify` at the repo root should not depend on CDK's type tree.",
  "scripts": {
    "synth": "cdk synth --strict",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all",
    "destroy": "cdk destroy --all"
  },
  "devDependencies": {
    "aws-cdk": "^2.1029.4",
    "aws-cdk-lib": "^2.221.0",
    "cdk-nag": "^2.37.19",
    "constructs": "^10.4.2",
    "tsx": "^4.20.3",
    "typescript": "^5.7.2"
  }
}
